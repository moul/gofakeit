name: Test

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]

jobs:
  test:
    strategy:
      matrix:
        go: [1.22.*]
        os: [ubuntu-latest, windows-latest]
        include:
          # macos-latest is Tahoe, which requires LC_UUID (Go 1.24+)
          - go: 1.24.*
            os: macos-latest
    runs-on: ${{ matrix.os }}

    name: Go ${{ matrix.go }} ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
    
      - name: Setup Go
        uses: actions/setup-go@v3
        with:
          go-version: ${{ matrix.go }}
    
      - name: Test
        run: go test ./...
